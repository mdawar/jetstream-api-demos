# Mirror for Work Queue Stream

Tests showing the errors when creating a consumer with a subject filter that conflicts with a mirror stream.

Both the new and the legacy JetStream APIs are included.

## Run the Tests

```sh
go test
```
